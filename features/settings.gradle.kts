pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "features"
 include(":shared")
 include(":feature1")
 include(":feature2")
 include(":feature3")
