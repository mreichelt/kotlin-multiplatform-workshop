pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "Habits"
include(":androidApp")
include(":shared")
include(":backend")
